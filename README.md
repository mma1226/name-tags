# Name Tags

A browser-based name tag / image-making tool. Everything runs client-side in a single self-contained HTML file — no build step, no server.

## Use it

Open the hosted version on GitHub Pages, or clone and open `index.html` locally:

```bash
open index.html
```

## Hosting

Served via GitHub Pages from the `main` branch root.
